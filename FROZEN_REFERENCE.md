## Reference to Old Project

This project (`crypto-alpha-lab`) is a successor to `my-bitcoin-project`.

The old project is frozen and located at:
```
/Users/tanshuai/Desktop/AI-test/my-bitcoin-project/
```

### What the old project contains (reference only)

- `src/execution/` — **Origin of this project's execution layer**
- `src/strategy/` — carry engine, MR engine, regime engine (not migrated)
- `src/nextgen_paper_runtime/` — diagnostic + artifact runtime (not migrated)
- `src/screening/`, `src/router/`, `src/buckets/` — governance surface (not migrated)
- `tests/` — origin of this project's execution test suite

### Why we stopped developing the old project

See `docs/roadmap.md` → "Background: Why We Left the Old System".
